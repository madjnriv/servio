import { DATABASE_ID, COLLECTION_ID } from "@/shared/constants/database";

const collectionId = COLLECTION_ID["SERVICE"];

export const servicesService = {
  create: () => {},
  getAll: async () => {},
  getById: async () => {},
  delete: async (id: string) => {},
};
